spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/sudden;AUTO_SERVER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create #Application ?? ??? Entity ?? & ???
    properties:
      hibernate:
        show_sql: true #??? SQL ? ? ?? :) System out ? ?? ??.
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug #sql ? ?? DEBUG ??? ???? ?. Logger ? ??.
    org.hibernate.type: trace



